
@Library('dst-shared@release/shasta-1.4') _

dockerBuildPipeline {
    repository = "cray"
    imagePrefix = "cray"
    app = "boa"
    name = "boa"
    description = "Cray management system boot orchestration agent"
    product = "csm"
    enableSonar = false
    sendEvents = ["BOA"]
}
